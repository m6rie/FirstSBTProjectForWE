package com.scala.exercices.dayfour

// represent a List with a sequence of number from 100 to 150

object SequenceHundred extends App {
  //  val range = 100 to 150
  //  var list = range.map(n => n)
  //  println(list)

    var liste = (100 until 150).toList
    println(liste)
}
