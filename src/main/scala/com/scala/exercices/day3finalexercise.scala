package com.scala.exercices

object day3finalexercise extends App {
  /*  // --- Doughnuts price ---
  val singleDoughnut: Double = 2.50
  var DoughnutsBox = (singleDoughnut * 10)
  println(f"The total cost of 10 Glazed Donuts is $DoughnutsBox%1.2f")

// --- Favorite movie of all time ---
  println("What is your favorite movie of all time?")
  var favemovie: String = scala.io.StdIn.readLine()
  println(s"\"Customer's favorite movie\" is $favemovie")

 // --- ID and Fave movie ---
  println("What is your first name?")
  var firstname: String = scala.io.StdIn.readLine()
  println("What is your last name?")
  var lastname: String = scala.io.StdIn.readLine()
  println("What is your favorite movie?")
  var movie: String = scala.io.StdIn.readLine()

  println(f"\nFirst name: $firstname\nLast name: $lastname\nFavourite Movie: $movie\n")

// --- Concatenation String from user's input ---
  println("Write something")
  var string1 = scala.io.StdIn.readLine()
  println("Write something else")
  var string2 = scala.io.StdIn.readLine()
  println(string1+string2)

  // --- Number of occurrences of a character in a string ---
  println("Write something: ")
  var userInput:String = scala.io.StdIn.readLine()
  var chars = userInput.toLowerCase.toList

  println("Which character would you like to count? ")
  var charInput = scala.io.StdIn.readChar().toLower

  var result = (chars.count(_ == charInput))

  if(result == 0 || result == 1){
    println(s"There is $result \"$charInput\" in your text.")
  } else {
    println(s"There are $result \"$charInput\" in your text.")
  }

//var charCount = 0
//for (i <- userInput) {
//    if(i == charInput){
//      count += 1
//    }
//  }

  //--- Reverse a (given) string ("Hello") ---
  def reverse(a:String) = {
    var reversedString:String = a.reverse
    println(reversedString)
  }
  reverse("Hello")

            //-----
  val randomString:String = "Hello"
  print(randomString.reverse)

  // --- Palindrome check ---
  def palindrome(p: String): Unit = {
    println("Check if a word is a palindrome")
    val p = scala.io.StdIn.readLine()
    val pRev = p.reverse
    if (p == pRev) {
      println("It is a palindrome.")
    } else {
      println("It is not a palindrome.")
    }
  }
  // palindrome("p")
  */

}